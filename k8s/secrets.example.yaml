# ============================================================
# TEMPLATE — copy this file to secrets.yaml and fill in values
# secrets.yaml is gitignored and must never be committed
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: thiscount-db-secret
  namespace: thiscount
type: Opaque
stringData:
  POSTGRES_USER: your-db-username
  POSTGRES_PASSWORD: your-db-password
